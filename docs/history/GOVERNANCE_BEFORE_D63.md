# تاریخچهٔ قواعد همکاری پیش از D63

> بایگانی؛ این فایل دستور اجرایی نیست. مرجع جاری: [وضعیت معتبر تصمیم‌ها](../02_DECISIONS.md#current-policy). متن زیر عین ردیف‌های پیش از اصلاح است و برای حفظ رد تصمیم‌ها نگهداری می‌شود.

| شناسه | موضوع | متن تاریخی | منشأ تاریخی |
|---|---|---|---|
| D34 | نقش‌ها و Governance | **مهدی**: Product Owner و داور نهایی. **پیکسل (Claude)**: مدیر پروژه + مالک معماری + مالک اسناد. **وی‌بانو (ChatGPT)**: بازبین مستقل معماری/کد و Quality Gate. **مکس (Claude Code)**: مهندس پیاده‌سازی. **جریان تغییرات مهم:** پیشنهاد پیکسل → بازبینی وی‌بانو → ثبت تصمیم (ADR) → پیاده‌سازی مکس → کنترل Scope توسط پیکسل → بازبینی ریسک/معماری توسط وی‌بانو → در اختلاف، مهدی تصمیم می‌گیرد. پیکسل هرگز کار خودش را تنها تأیید نمی‌کند. ADR اجباری برای: استراتژی schema، Auth، مدل پول، رزرو موجودی، معماری پرداخت، Provider جست‌وجو/AI، هاست، معماری بکاپ | تأیید هر دو بازبین |
| D39 | نسخهٔ Baseline | نسخهٔ اسناد در `00_INDEX.md` ثبت می‌شود (فعلاً **v1.2**). هر PR مکس می‌نویسد: `Implemented against docs v1.2 / Dxx`. تغییر Baseline فقط با تأیید پیکسل + وی‌بانو | ردیابی Drift |
| D44 | انضباط برنچ روی `main` | GitHub روی یک **مخزن خصوصی در پلن Free** هیچ branch protection یا ruleset را اجرا نمی‌کند (تأییدشده: همان بنر «فعال نیست» برای هر دو نوع، classic rule و ruleset). ruleset موجود است ولی بی‌اثر، و به سازمان پولی نمی‌رویم. پس این قوانین باید در اسناد زندگی کنند و رعایت شوند (منعکس در `AGENTS.md` §5، شکستنشان خطای جدی است نه میان‌بر): **(۱)** هرگز مستقیم روی `main` commit/push نکن — هر تغییر از برنچ + PR رد می‌شود، حتی یک‌خطی docs (تنها استثنا: همین کامیت housekeeping فاز ۰۰ که مهدی صریح اجازه داد). **(۲)** هرگز PR خودت را merge نکن — merge کارِ مالک است، بعد از بازبینی Pixel و Vee. **(۳)** هرگز وقتی یک job CI قرمز یا در حال اجراست درخواست بازبینی نده. **(۴)** هرگز روی برنچ مشترک force-push نکن و تاریخچهٔ push‌شده را بازنویسی نکن. اگر مخزن روزی public شود یا به پلن پولی برود، ruleset موجود خودبه‌خود فعال می‌شود. **به‌روزرسانی:** با D45 مخزن public شد و این قوانین حالا به‌صورت فنی هم اجرا می‌شوند. | چیزی به‌صورت خودکار `main` را محافظت نمی‌کند؛ تأیید Pixel + Vee، ۲۰۲۶-۰۹-۰۳ |
| D45 | مخزن public؛ انضباط برنچ فنی | مخزن به تصمیم مهدی public می‌ماند (دسترسی هر سه عامل بدون احراز هویت)، به شرط پاک‌سازی اطلاعات شخصی و حذف اسناد تجاری کامل (PR #2). چون branch protection روی مخزن public فعال است، مهدی روی `main` یک Ruleset فعال می‌کند: Require pull request؛ Require status checks `checks` / `docker` / `docker-runtime`؛ Block force pushes؛ Restrict deletions؛ Require conversation resolution. قوانین رفتاری D44 بدون تغییر می‌مانند؛ فرض «مخزن private» در D44 با این ردیف اصلاح می‌شود. | مهدی؛ پیشنهاد پیکسل؛ تأیید وی‌بانو |
| D46 | زمان‌بندی زیرساخت واقعی | پیش از **Checkpoint 1**: سرور staging + دامنه‌ی آزمایشی + HTTPS واقعی Caddy + S3 واقعی (R2/MinIO) — چون ویترین و تصاویر در CP1 تست می‌شوند. پیش از **Checkpoint 2**: بکاپ off-site (OB6) + یک restore drill واقعی روی staging + MFA (فاز ۰۵). پیش از **Production**: تست دسترسی از ایران/ترکیه/کانادا (OB7) + مسیر کامل خرید. وی‌بانو، ۵ سپتامبر ۲۰۲۶. | مهدی؛ پیشنهاد پیکسل؛ تأیید وی‌بانو |
| D47 | جریان کاری پس از توقف Claude | از ۸ سپتامبر ۲۰۲۶، **Codex implementation agent** مجری کد و تست است و **وی‌بانو (ChatGPT)** مدیریت پروژه و بازبینی مستقل خروجی Codex را بر عهده دارد. پیکسل/Claude و مکس/Claude Code دیگر جزء اجباری چرخه نیستند؛ گزارش‌های تاریخی آن‌ها حفظ می‌شود. ربات GitHub با نام `chatgpt-codex-connector` فقط بازبین خودکار اضافی است و با Codex مجری یکی نیست. همهٔ تغییرها همچنان branch + PR، بدون force-push و با سه CI سبز انجام می‌شوند. هیچ PR بدون اجازهٔ جداگانه و صریح مهدی Merge نمی‌شود؛ پس از آن اجازه، وی‌بانو می‌تواند Merge را از طرف مالک اجرا و نتیجه را راستی‌آزمایی کند. این ردیف فقط بخش نقش‌ها/چرخهٔ D34 و D44 را به‌روز می‌کند و هیچ تصمیم معماری را تغییر نمی‌دهد. | تصمیم صریح مهدی؛ ثبت وی‌بانو، ۲۰۲۶-۰۹-۰۸ |
| D49 | تعویق موقت زیرساخت و پذیرش دستی CP1 | به‌دلیل آماده‌نبودن سرور و دامنه، بخش عملیاتی Checkpoint 1 (staging، دامنهٔ آزمایشی، HTTPS واقعی Caddy، Lighthouse عمومی و ۱۷ آزمون دستی روی گوشی/لپ‌تاپ) فعلاً باز می‌ماند، اما پس از سبزشدن کامل CI فاز ۰۲ مانع شروع فازهای ۰۳ تا ۰۵ نیست. این استثنا فقط زمان‌بندی D46 را تغییر می‌دهد و هیچ معیار پذیرشی حذف یا پاس‌شده فرض نمی‌شود. همهٔ موارد CP1 و پیش‌نیازهای عملیاتی D46 باید حداکثر پیش از اجرای دستی Checkpoint 2 تکمیل شوند؛ اگر CP1 بیش از سه باگ مسدودکننده آشکار کند، قاعدهٔ توقف رودمپ فوراً اعمال می‌شود. | تصمیم صریح مهدی؛ ثبت و تأیید وی‌بانو، ۲۰۲۶-۰۹-۰۹ |
| D50 | اختیار اجرایی مالک | مالک در ۱۰ سپتامبر ۲۰۲۶ اختیار تصمیم‌های فنی و ادغام PRهای لازم را به عامل اجرایی واگذار کرد؛ اجازهٔ جداگانه برای هر ادغام لازم نیست و عامل می‌تواند PR پیاده‌سازی‌شدهٔ خود را پس از بازبینی و تأیید کیفیت ادغام کند. شرط branch + PR، بررسی امنیتی، سه CI سبز، منع force-push و گزارش صریح نتایج باقی است. بازبینی خودکار GitHub مکمل کنترل کیفیت است و نباید به‌عنوان بازبینی انسانی معرفی شود. این ردیف الزام اجازهٔ جداگانه و منع ادغام توسط مجری در D44/D47 را جایگزین می‌کند؛ سیاست دسترسی کاربران محصول و D25 تغییر نمی‌کند. | دستور صریح مالک در همین جلسه؛ ۲۰۲۶-۰۹-۱۰ |
| D51 | خرید زیرساخت نزدیک انتشار | توسعه و تست خودکار فازها می‌تواند پیش از خرید هاست و دامنه ادامه یابد. خرید و استقرار به مرحلهٔ نزدیک انتشار منتقل می‌شود؛ پذیرش دستی CP1/CP2، HTTPS، ایمیل واقعی، off-site و تست دسترسی سه کشور همچنان پیش‌نیاز انتشارند و بدون اجرا تأییدشده محسوب نمی‌شوند. این ردیف فقط زمان‌بندی D46/D49 را به‌روز می‌کند. | تصمیم مالک و پاسخ مورد قبول او در همین جلسه؛ ۲۰۲۶-۰۹-۱۰ |

## تاریخچهٔ نسخه‌های اولیه

> v1.1 (بعد از بازبینی دور اول وی‌بانو): D04، D06، D14، D22، D23، D24 اصلاح و D31–D36 اضافه شد. **v1.1.1 (دور دوم):** D23 و D34 اصلاح، D37–D39 اضافه. **v1.1.2 (دور سوم):** دو Hardening در اسکریپت‌ها + D40. **v1.1.3 (دور چهارم):** باگ محاسبهٔ حجم zip، Fail-closed، سقف tar، تست‌های خودکار (D41). **v1.2 (دور پنجم):** هفت پیشنهاد وی‌بانو (V-1…V-7) در D17/D21/D25 و فازها اعمال شد. **v1.2.1 (بستن فاز ۰۰، ۲۰۲۶-۰۹-۰۳):** D42 (نشست JWT ادمین)، D43 (rollback واقعی دیپلوی → فاز ۰۵)، D44 (انضباط برنچ روی `main`) اضافه شد — تأیید Pixel + Vee. baseline معماری در `00_INDEX.md` تغییری نکرد. تاریخچه در `10…14_REVIEW_*.md`.
